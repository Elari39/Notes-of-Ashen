# 仅供 scripts/test-integration.ps1 的 Redis 密码错误启动验收使用。
# Redis 继续读取 APP_REDIS_PASSWORD；仅 API 读取单独的错误密码，验证其启动期 fail-fast。
services:
  api:
    environment:
      APP_REDIS_PASSWORD: "${E2E_API_REDIS_PASSWORD:?set E2E_API_REDIS_PASSWORD for the Redis authentication failure test}"
