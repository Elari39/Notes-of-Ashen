# Notes of Ashen 项目审计报告（第二轮）

**审计日期**：2026-06-28
**审计范围**：全项目（后端 Go + 前端 React + 部署配置 + 接口一致性 + 文档）
**审计深度**：全面深度审计，按 P0-P3 优先级分级
**本轮目标**：对上一轮 27 项闭环后的代码库做全新审计，闭环修复全部 P1、绝大部分 P2、精选 P3，并将所有发现（含未修复项）记录归档

---

## 总体评价

项目整体工程质量保持较高水准。后端安全防护（SQL 全参数化、鉴权、密钥 fail-closed、IP 信任链、Token 旋转撤销、限流 fail-closed）、事务一致性、降级路径、首注册 admin 并发保险等良好实践均持续生效。本轮**未发现 P0 级问题**。

新发现的问题集中在四个方向：① 部署必填项与默认值在搜索/RabbitMQ 关闭场景下的首次部署摩擦（P1）；② 定时发布状态机在状态切换接口下的 `scheduled_at` 残留（P2）；③ 前端 UX 细节（操作按钮 loading 不区分、角色切换无确认、改密无确认密码）与零散 i18n 硬编码（P2/P3）；④ 接口 nil 切片序列化导致前端数组类型收到 `undefined`（P2）。

本轮共发现 24 项问题，已全部闭环：20 项直接修复（全部 P1、全部接口/状态机类 P2、全部前端 UX 类 P2/P3、P2-8 i18n 统一迁移、P3-13 nginx CSP），4 项经核实已修复或无需改造（aiclient 超时热更、Refresh TOCTOU、PopularArticles 状态过滤等）。

---

## P1 — 首次部署 / 本地启动阻断 / 安全

### ✅ P1-1 [部署] APP_MEILISEARCH_API_KEY 使用 `:?` 强制必填，但搜索默认关闭
- **问题文件**：`docker-compose.yml:62`
- **问题描述**：搜索默认关闭（`APP_SEARCH_ENABLED:-false`）、Meilisearch 容器在 `profiles:["search"]` 下才启动，但 api 服务的 `APP_MEILISEARCH_API_KEY` 仍用 `:?` 必填标记。compose 解析阶段变量未设置即报错，导致不使用搜索的用户无法 `docker compose up`。应用层 `ValidateConfig`（`config.go:271`）已条件校验，compose 层 `:?` 多余。
- **修复**：`:?` → `:-`（空默认值），由应用层按 `Search.Enabled` 决定是否需要。

### ✅ P1-2 [部署] meilisearch 容器 MEILI_MASTER_KEY 同样 `:?` 必填
- **问题文件**：`docker-compose.yml:116`
- **问题描述**：同 P1-1，容器在 search profile 下启动，但 `:?` 在 compose 解析阶段阻断。
- **修复**：`:?` → `:-`。

### ✅ P1-3 [部署] APP_RABBITMQ_ENABLED 默认 true，但 compose 无 RabbitMQ 服务
- **问题文件**：`docker-compose.yml:64`、`etc/notes-of-ashen.yaml:42`、`.env.example:32`、`README.md:146`
- **问题描述**：四方默认值不一致（compose/yaml 默认 `true`，.env.example 默认 `false`，README 写 `true`）。compose 不含 rabbitmq 服务，默认启用时 `NewPublisher` 连接失败、无限重连刷日志；本地 `go run` 因 `ValidateConfig` 要求 `APP_RABBITMQ_URL` 而启动失败。
- **修复**：四方统一为 `false`（更安全默认，需要时显式启用）。

### ✅ P1-4 [部署] Dockerfile.api.dockerignore 未排除敏感文件
- **问题文件**：`Dockerfile.api.dockerignore`
- **问题描述**：BuildKit 优先按 Dockerfile 名匹配 `*.dockerignore`。该文件仅排除前端产物，未排除 `.env`、`*.pem`、`*.key`、`*.crt` 等，导致 `COPY . .` 将含真实密钥的 `.env` 带入构建上下文/镜像。
- **修复**：追加 `.env`、`.env.*`、`!.env.example`、`*.pem`、`*.key`、`*.crt`、`*.p12`、`*.pfx`、`*.keystore`、`secrets/`。

### ✅ P1-5 [文档] README 端口映射描述错误 `-> 80` 实际为 `-> 8080`
- **问题文件**：`README.md:317`
- **问题描述**：容器内 nginx 实际 `listen 8080`（`deploy/nginx/default.conf`、`Dockerfile.web EXPOSE 8080`、compose `127.0.0.1:1270:8080`），文档写 `-> 80`。
- **修复**：改为 `-> 8080`。

---

## P2 — 接口/状态机不一致 / 明显 UX 问题

### ✅ P2-1 [后端] UpdateArticleStatus 不清空 scheduled_at，定时发布状态机残留
- **问题文件**：`model/article.go:192-215`（`UpdateArticleStatus`）、`:823`（`publishedAtForUpdate`）、`:839`（`IsArticlePubliclyVisible`）
- **问题描述**：`PATCH /articles/:id/status` 只更新 `status`、`published_at`，从不修改 `scheduled_at`。对「定时中的文章」（`status=published` + 未来 `scheduled_at`）用状态接口直接改状态后，`scheduled_at` 仍为未来时间，`IsArticlePubliclyVisible` 判定不可见——用户以为已发布实际看不到。
- **修复**：状态接口语义为「立即切换」，UPDATE 一律 `SET scheduled_at = NULL`，且 `publishedAtForUpdate` 不再传 `scheduledAt`（转 published 取原首发时间或 now）。定时发布仍走 `UpdateArticle`（PUT）。

### ✅ P2-2 [接口] ArticleVersionResp.TagIDs / ArticleResp.Tags nil 序列化导致前端收到 undefined
- **问题文件**：`internal/logicutil/common.go:124-150`、`internal/types/types.go:287,396`
- **问题描述**：DB 中 `tag_ids` 为空时 `scanArticleVersion` 得到 nil，叠加 `omitempty`，JSON 省略 `tagIds`/`tags` 字段；前端类型为非可选数组，收到 `undefined`。
- **修复**：`logicutil` 中 `TagIDs` 用 `nonNilUint64Slice` 兜底为 `[]uint64{}`；`ArticleResp.Tags` 用 `make([]TagResp, 0, len(tags))` 初始化，保证输出 `[]`。

### ✅ P2-3 [接口] 操作日志仅返回数字 userId，无用户名
- **问题文件**：`model/log.go`、`internal/logicutil/common.go:157`、`api/notes-of-ashen.api:475`、`internal/types/types.go:429`、`frontend/src/types/index.ts:225`、`frontend/src/pages/admin/Logs.tsx:82`、`Dashboard.tsx LogRow`
- **问题描述**：`OperationLogResp` 只有 `userId`（数字），管理员无法知道操作发起者是谁。
- **修复**：后端 `ListOperationLogs` SQL `LEFT JOIN users` 取 `account`；`OperationLog`/`OperationLogResp` 增加 `UserAccount` 字段（四方同步：.api、types.go、logicutil、docs/API.md、前端类型）；前端 Logs 列表与 Dashboard LogRow 展示 `userAccount || userId || '-'`。

### ✅ P2-4 [前端] getUserRoleLabel 缺 editor 角色
- **问题文件**：`frontend/src/i18n.ts:655`、`frontend/src/pages/admin/Users.tsx:132`
- **问题描述**：`UserRole` 含 `editor`，但 `getUserRoleLabel` 只覆盖 admin/user，editor 在 Users.tsx 内联硬编码绕过，逻辑分散且他处调用会返回原始 `'editor'`。
- **修复**：`getUserRoleLabel` 补 `editor`，Users.tsx 统一调用。

### ✅ P2-5 [前端] 后台文章列表操作按钮 loading 不区分
- **问题文件**：`frontend/src/pages/admin/Articles.tsx:364-377`
- **问题描述**：`busyId===a.id` 禁用全部操作按钮，点击「导出」后「删除」也变灰，用户无法区分当前操作。
- **修复**：改为 `busy: {id, action}`，仅禁用执行中按钮并加 `…` 后缀作 spinner 提示，其余按钮可点。

### ✅ P2-6 [前端] 用户管理角色切换无确认提示
- **问题文件**：`frontend/src/pages/admin/Users.tsx:78-89`
- **问题描述**：`handleRole` 直接 `onChange` 改角色，误触即降级/提权 admin，与有 `confirm()` 的 `handleStatus` 不一致。
- **修复**：`handleRole` 加 `confirm()`（新增 `users.confirmRole` i18n key），busy 同 C2 区分 status/role。

### ✅ P2-7 [前端] 双图表库 recharts + echarts 功能重叠
- **问题文件**：`frontend/package.json`、`frontend/src/pages/admin/Dashboard.tsx`
- **问题描述**：recharts 仅用于 Dashboard 流量折线图，echarts 用于 Resume 技能图谱，两库体积大且重叠。
- **修复**：Dashboard 折线图改用 echarts（tree-shakeable：`LineChart`/`GridComponent`/`TooltipComponent`/`LegendComponent`/`CanvasRenderer`），移除 recharts 依赖与 vite chunk 分组。

### ✅ P2-8 [前端] i18n 文案散落 15+ 文件内联，未走统一字典
- **问题文件**：`Home.tsx`、`ArticleDetail.tsx`、`ArticleEditor.tsx`、`Articles.tsx`、`ArticleVersions.tsx`、`Settings.tsx`、`AISettings.tsx`、`Dashboard.tsx`、`Resume.tsx`、`Projects.tsx`、`ResumeContent.tsx`、`ProjectsContent.tsx`、`ArticlePreview.tsx`、`ProjectPreviewModal.tsx`、`ImageLightbox.tsx`、`MarkdownTable.tsx`、`utils/error.ts` 等
- **问题描述**：大量 UI 文案通过组件内 `language === 'zh' ? {...} : {...}` 硬编码，独立于 `i18n.ts` 的 `translate()` 机制，维护困难、无法集中审计、新增语言需改每个文件。
- **修复**：将 18 个文件的内联双语全部下沉到 `i18n.ts` 统一字典（新增约 200 个 zh/en key，按功能域分配前缀 `articleAdmin.*`/`projectAdmin.*`/`resumeAdmin.*`/`articleVersion.*`/`aiSettings.*`/`articleDetail.*`/`articleToc.*`/`projects.*`/`resume.*`/`projectModal.*`/`imageLightbox.*`/`markdownTable.*`/`articlePreview.*`/`aiAction.*`/`error.field*` 等）。函数型 label（`viewImage(title)`、`confirmRestore(versionNo)`、`resultReady(action)`、`projectNo(value)`、`itemNo(value)`）转为 `{placeholder}` 模板 + `formatText`。跨子组件传 `labels` prop 的（`ProjectCard`、`SkillGraphSection`、`ProjectEditor`、各 Resume Editor）改为子组件内部 `usePreferenceStore` 订阅。`utils/error.ts` 的 5 处 field 三元迁移到 `error.field*` key，`defaultFallbacks` 改用 `translate(language, 'error.operationFailedFallback')`，`exactMessages`/`fieldNames` 表结构保留。所有文件级 `xxxLabels(language)` 辅助函数已删除。

---

## P3 — 局部 polish / 维护性

### ✅ P3-1 [后端] ChangePassword 未驱逐 auth 用户缓存
- **问题文件**：`internal/logic/user/user.go:155-188`
- **问题描述**：改密后只 `RevokeUserRefreshTokens`，未像 `admin.go:65/95` 那样 `EvictAuthUserCache`。当前缓存仅含 role/status 非必须，但作防御性一致。
- **修复**：成功后调用 `middleware.EvictAuthUserCache`。

### ✅ P3-2 [后端] articleResp 分类缺失直接 500，未降级
- **问题文件**：`internal/logic/article/article.go:670-683`
- **问题描述**：`FindCategory` 返回 `ErrNotFound`（如直接删库未清 `category_id`）时详情页 500，而批量 `articlesResp` 降级为空。
- **修复**：`ErrNotFound` 时 `category = nil` 继续返回，其它错误仍向上抛，与批量容错一致。

### ✅ P3-3 [前端] ArticleEditor 标题/Slug 无必填校验
- **问题文件**：`frontend/src/pages/admin/ArticleEditor.tsx:624-633`
- **问题描述**：`handleSave` 仅校验 coverUrl，留空标题/slug 直接发后端报错，缺即时反馈。
- **修复**：补 `title.trim()`/`slug.trim()` 校验（新增 `articleEditor.titleRequired`/`slugRequired` i18n key）。

### ✅ P3-4 [前端] ArticleEditor SEO placeholder 硬编码英文
- **问题文件**：`frontend/src/pages/admin/ArticleEditor.tsx:889,896,903`
- **修复**：`getAIEditorLabels` 加 `seoTitle`/`seoDescription`/`seoKeywords`，placeholder 走 i18n。

### ✅ P3-5 [前端] Profile 改密缺少确认新密码
- **问题文件**：`frontend/src/pages/Profile.tsx:128-145`
- **问题描述**：仅 oldPassword/newPassword，无确认字段，打错无二次校验。
- **修复**：加 `confirmPassword` + `match` 校验规则，对齐 Register/ForgotPassword。

### ✅ P3-6 [前端] ArticleDetail 点赞未采纳后端 liked
- **问题文件**：`frontend/src/pages/ArticleDetail.tsx:172-188`
- **问题描述**：点赞接口返回 `liked`，前端忽略直接 `setHasLiked(true)`。
- **修复**：以 `res.data.liked ?? true` 为准，localStorage 仅辅助记忆。

### ✅ P3-7 [前端] AdminLayout "ADMIN" 标签硬编码
- **问题文件**：`frontend/src/pages/admin/AdminLayout.tsx:82`
- **修复**：改用 `t('admin.title')`（zh「管理」/ en「Admin」）。

### ✅ P3-8 [前端] refresh.ts 硬编码中文错误消息
- **问题文件**：`frontend/src/utils/refresh.ts:27`
- **问题描述**：fallback `'登录已过期，请重新登录'` 在英文环境直接展示中文。
- **修复**：改用 `ERROR_KEYS.sessionExpired`，由 `error.ts` `translateMessage` 统一本地化。

### ✅ P3-9 [前端] ProtectedRoute 废弃 requireAdmin prop
- **问题文件**：`frontend/src/components/ProtectedRoute.tsx:9-35`
- **问题描述**：`requireAdmin` 已被 `allowedRoles` 取代，仅作 fallback，增加接口歧义。
- **修复**：移除 `requireAdmin`，统一用 `allowedRoles`（App.tsx 已全部使用）。

### ✅ P3-10 [前端] ArticleVersion.tagIds 类型精度
- **问题文件**：`frontend/src/types/index.ts:187`
- **修复**：改为 `tagIds?: number[]`（与后端 omitempty 对齐，P2-2 已保证运行时为 `[]`）。

### ✅ P3-11 [文档] README 增量脚本列表遗漏 add_operation_logs_index.sql
- **问题文件**：`README.md:185-199`
- **修复**：追加第 14 项。

### ✅ P3-12 [文档] README alter_site_settings_value_text.sql 描述「改 TEXT」实为 MEDIUMTEXT
- **问题文件**：`README.md:191`
- **修复**：改为「改 MEDIUMTEXT」。

### ✅ P3-13 [部署] nginx 缺少 Content-Security-Policy 头
- **问题文件**：`deploy/nginx/default.conf:57-59`、`deploy/nginx/security-headers.conf`（新增）、`Dockerfile.web`
- **问题描述**：原 nginx 仅在 server 块顶层有 `X-Content-Type-Options`/`X-Frame-Options`/`Referrer-Policy` 三个安全头，无 CSP；且静态资源 location 与 `= /index.html` location 各自有 `add_header Cache-Control`，触发 nginx `add_header` 继承陷阱（子 location 出现 `add_header` 时父级全部失效），导致这两个 location 的响应连上述三个安全头都不携带。
- **修复**：抽取 `deploy/nginx/security-headers.conf` 片段（4 条 `add_header`，含 CSP，均 `always`），放 `/etc/nginx/snippets/`（不被 `conf.d/*.conf` 通配自动 include）。server 块顶层与两个带 `Cache-Control` 的 location 内显式 `include` 该片段，覆盖所有路径。CSP 策略：`default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'`（React 行内 style 属性兼容）`; img-src 'self' data: blob: https: http:`（验证码 data URI / 导出 blob / 用户内容外部图床）`; ...; worker-src 'self'`（SW 同源）`; frame-ancestors 'self'; upgrade-insecure-requests`。`Dockerfile.web` 增加 `COPY security-headers.conf /etc/nginx/snippets/`。
- **验证**：nginx 镜像实测 `/`、`/index.html`、静态资源、`/api/` 四类响应均携带全部 4 个安全头（此前静态资源/首页缺失的三个安全头已恢复）。

### ✅ P3-14 [前端] Resume/ProjectsContent 等默认标题硬编码中文 fallback
- **问题文件**：`frontend/src/pages/admin/ProjectsContent.tsx:12-16`、`ResumeContent.tsx:12-19`
- **修复**：`emptyResume`/`emptyProjects` 的 `title` 改为 `''`；`getResumeAdminLabels`/`getProjectsAdminLabels` 新增 `defaultTitle` 双语 key；`normalizeResumePage`/`normalizeProjectsPage` 改为接收 `defaultTitle` 参数，调用点（含初始加载 useEffect 与保存重载）传入 `text.defaultTitle`（useEffect 内改用 `getResumeAdminLabels(languageRef.current).defaultTitle` 以避免 exhaustive-deps 警告，与既有 languageRef 模式一致）。附带修复 `ProjectsContent.tsx` TagSelector 的裸英文 `'No tags'`，新增 `noTags` 双语 key。

### ✅ P3-15 [前端] 移动端偏好设置面板高度可能溢出
- **问题文件**：`frontend/src/components/Layout.tsx:151-153`
- **修复**：移动端偏好面板 `max-h-[calc(100vh-7rem)]` 改为 `max-h-[calc(100dvh-7rem-env(safe-area-inset-bottom))]` 并叠加 `pb-[env(safe-area-inset-bottom)]`。`100dvh` 随移动浏览器 Chrome 伸缩避免底部被遮挡，safe-area 内边距避免被底部手势条裁切。

---

## 审计中确认的良好实践（持续有效，无需修复）

| 维度 | 评价 |
|------|------|
| SQL 注入防护 | model 层全参数化，`IN (...)` 动态占位符，LIKE 经 `escapeLike` 转义 |
| 认证授权 | 受保护路由全过 `authRequired`，`RequireAdmin`/`RequireContentManager` 在 logic 入口统一 |
| 密钥安全 | `ValidateConfig` fail-closed 拦截占位符，AI Key AES-GCM 加密 |
| IP 信任链 | 先验 `RemoteAddr` 命中可信 CIDR 才用 XFF，从右向左取首个不可信 IP |
| Token 安全 | refresh 旋转（用后撤销）+ DB 原子撤销保证一次使用，DB 为准 + Redis 缓存降级 |
| 防枚举 | 登录用户不存在/密码错误返回相同信息 |
| 限流 | 登录/注册/验证码/重置/改密 fail-closed 限流，Redis 故障返回 503 |
| 首注册 admin 并发 | `WithUserRegistrationLock`（GET_LOCK）+ `AdminExists` DB 双保险 |
| 文章版本管理 | `UpdateArticle`/`UpdateArticleStatus`/`RestoreArticleVersion` 事务内先快照 |
| 事务一致性 | 文章/分类/标签/站点设置/简历项目增删改包在 `WithTx` 内 |
| 缓存降级 | auth 用户缓存 Redis 故障 fail-open 直查 DB，refresh token 缓存缺失以 DB 为准 |
| 降级路径 | RabbitMQ 禁用/失败回退同步落库；Search 禁用全 no-op；Email 禁用 Forbidden；AI 禁用跳过校验 |
| 统一响应 | 所有 JSON handler 统一 `response.Ok`/`NoData`/`ErrorCtx` |
| 前端 HTTP 层 | token 刷新 + 并发去重 + 分级超时设计完善 |
| 前端状态覆盖 | Loading/Error/Empty State 覆盖全面，路由守卫清晰 |
| schema.sql 幂等 | `IF NOT EXISTS` + `ON DUPLICATE KEY UPDATE`，字符集 `utf8mb4_0900_ai_ci` |
| /healthz | 实现与 `docs/API.md` 一致（JSON 报告 + 200/503 语义） |

---

## 本轮修复记录（2026-06-28）

### 后端
- `model/article.go`：`UpdateArticleStatus` UPDATE 改为 `SET scheduled_at = NULL`，`publishedAtForUpdate` 不再传 `scheduledAt`（P2-1）
- `model/log.go`：`ListOperationLogs` LEFT JOIN users 取 account；`OperationLog` 加 `UserAccount`；`scanOperationLog` 增列；同步 `log_test.go`（P2-3）
- `internal/logicutil/common.go`：新增 `nonNilUint64Slice`，`ArticleVersionResp.TagIDs`/`ArticleResp.Tags` 兜底空切片；`OperationLogResp` 透传 `UserAccount`（P2-2/P2-3）
- `internal/types/types.go`、`api/notes-of-ashen.api`：`OperationLogResp` 加 `UserAccount` 字段（P2-3）
- `internal/logic/user/user.go`：`ChangePassword` 成功后 `EvictAuthUserCache`（P3-1）
- `internal/logic/article/article.go`：`articleResp` 对 `FindCategory` 的 `ErrNotFound` 降级（P3-2）

### 部署 / 配置 / 文档
- `docker-compose.yml`：`APP_MEILISEARCH_API_KEY`/`MEILI_MASTER_KEY` `:?`→`:-`；`APP_RABBITMQ_ENABLED` 默认 `false`（P1-1/P1-2/P1-3）
- `etc/notes-of-ashen.yaml`：`RabbitMQ.Enabled` 默认 `false`（P1-3）
- `Dockerfile.api.dockerignore`：追加敏感文件排除（P1-4）
- `README.md`：端口 `->8080`、RabbitMQ 默认 `false`、增量脚本补第 14 项、`alter_site_settings` 改 MEDIUMTEXT（P1-3/P1-5/P3-11/P3-12）
- `docs/API.md`：操作日志补 `userAccount` 字段说明（P2-3）

### 前端
- `frontend/src/types/index.ts`：`Log` 加 `userAccount?`；`ArticleVersion.tagIds` 改可选（P2-3/P3-10）
- `frontend/src/pages/admin/Logs.tsx`、`Dashboard.tsx`：展示 `userAccount`（P2-3）
- `frontend/src/i18n.ts`：`getUserRoleLabel` 补 `editor`；新增 `users.confirmRole`/`articleEditor.titleRequired`/`slugRequired` key（P2-4/P2-6/P3-3）
- `frontend/src/pages/admin/Users.tsx`：busy 区分 status/role；角色切换加确认；editor 走 i18n（P2-4/P2-6）
- `frontend/src/pages/admin/Articles.tsx`：busy 区分 export/status/delete，按钮加 `…` 提示（P2-5）
- `frontend/src/pages/admin/ArticleEditor.tsx`：handleSave 加标题/slug 校验；SEO placeholder 走 i18n（P3-3/P3-4）
- `frontend/src/pages/Profile.tsx`：改密加 confirmPassword + match 校验（P3-5）
- `frontend/src/pages/ArticleDetail.tsx`：采纳后端 `liked`（P3-6）
- `frontend/src/pages/admin/AdminLayout.tsx`：ADMIN 标签走 i18n（P3-7）
- `frontend/src/utils/refresh.ts`：fallback 改用 `ERROR_KEYS.sessionExpired`（P3-8）
- `frontend/src/components/ProtectedRoute.tsx`：移除 `requireAdmin`（P3-9）
- `frontend/src/pages/admin/Dashboard.tsx`：recharts → echarts（tree-shakeable）（P2-7）
- `frontend/package.json`：移除 recharts；`frontend/vite.config.ts`：移除 recharts chunk 分组（P2-7）

### 验证
- `go build ./...` ✅
- `go test ./...` ✅（含 model/log_test.go 更新）
- `pnpm install`（移除 recharts）→ `pnpm lint` ✅ → `npx tsc --noEmit` ✅ → `pnpm build` ✅（无 chunk size warning）
- `docker compose config --quiet` ✅、`docker compose --profile search config --quiet` ✅
- 手动核对 `OperationLogResp` 四方（api/types/logicutil/docs/前端）字段一致 ✅
- 核对 RabbitMQ 默认值四方（yaml/.env.example/compose/README）均为 `false` ✅

---

## 后续修复记录（P2-8 / P3-13 闭环）

### P3-13 nginx CSP + add_header 继承陷阱
- 新增 `deploy/nginx/security-headers.conf`：4 条 `add_header`（X-Content-Type-Options / X-Frame-Options / Referrer-Policy / Content-Security-Policy），均 `always`
- `deploy/nginx/default.conf`：server 块顶层及静态资源 location、`= /index.html` location 显式 `include /etc/nginx/snippets/security-headers.conf;`，修复此前子 location 因自带 `add_header Cache-Control` 导致三个安全头失效的继承陷阱
- `Dockerfile.web`：增加 `COPY deploy/nginx/security-headers.conf /etc/nginx/snippets/security-headers.conf`
- 验证：nginx 镜像实测 `/`、`/index.html`、静态资源、`/api/` 四类响应均携带全部 4 个安全头 ✅

### P2-8 i18n 统一迁移
- `frontend/src/i18n.ts`：新增约 200 个 zh/en key（`common.pinned`、`imageLightbox.*`、`markdownTable.*`、`articlePreview.*`、`articleVersion.*`、`settings.*` 扩充、`aiSettings.*`、`dashboard.*` traffic 扩充、`articleEditor.*` AI/pin/scheduled 扩充、`aiAction.*`、`articleDetail.*`、`articleToc.*`、`projects.*`、`resume.*`、`projectAdmin.*`、`resumeAdmin.*`、`articleAdmin.*`、`projectModal.*`、`error.field*`、`error.operationFailedFallback`）
- 18 个文件下沉内联双语到 `translate()`，删除全部文件级 `xxxLabels(language)` 辅助函数：`Home.tsx`、`Search.tsx`、`ImageLightbox.tsx`、`MarkdownTable.tsx`、`ArticlePreview.tsx`、`ArticleVersions.tsx`、`Settings.tsx`、`AISettings.tsx`、`Dashboard.tsx`、`ArticleEditor.tsx`、`ArticleDetail.tsx`、`Projects.tsx`、`Resume.tsx`、`ProjectsContent.tsx`、`ResumeContent.tsx`、`Articles.tsx`、`ProjectPreviewModal.tsx`、`utils/error.ts`
- 函数型 label（`viewImage`/`confirmRestore`/`resultReady`/`resultTitle`/`projectNo`/`itemNo`/`localDraftDesc`）转为 `{placeholder}` 模板 + `formatText`
- 跨子组件传 `labels` prop 的（`ProjectCard`、`SkillGraphSection`、`ProjectEditor`、`ResumeArraySection`/`ExperienceEditor`/`EducationEditor`/`SkillEditor`/`EditorHeader`/`DateRangeInputs`）改为子组件内部 `usePreferenceStore` 订阅
- `utils/error.ts`：5 处 field 三元迁移到 `error.field*` key + `formatText`；`defaultFallbacks` 改用 `translate(language, 'error.operationFailedFallback')`；`exactMessages`/`fieldNames` 表结构保留

### 验证
- `npx tsc --noEmit` ✅（每批 + 最终）
- `pnpm build` ✅（lint + tsc + vite build，无 chunk size warning）
- `docker compose config --quiet` ✅、`docker compose --profile search config --quiet` ✅
- 残留扫描：`language === 'zh' ?` 仅余合法用途（locale 映射、语言切换逻辑、className 条件、error.ts 安全网逻辑分支、Layout 用 t() 二选一），无内联文案残留 ✅

---

## 技术债务追踪（均已闭环）

1. **P2-8 i18n 统一迁移**：已修复——18 个文件内联双语标签全部迁移至 `i18n.ts` 字典，统一 `translate()` 调用，删除所有文件级 `xxxLabels(language)` 辅助函数（详见 P2-8 条目）。
2. **P3-13 nginx CSP**：已修复——新增 `security-headers.conf` 片段并解决 `add_header` 继承陷阱，CSP 已上线（详见 P3-13 条目）。
3. **aiclient `sharedHTTPClient` 超时不可热更**（`internal/aiclient/client.go`）：已修复——`sync.Once` 单例改为按 `headerTimeout` 互斥复用/重建，超时变化时重建 Client；context 总超时已兜底。
4. **`Refresh` Redis 缓存 TOCTOU**（`internal/logic/auth/auth.go:280-329`）：经核实当前实现已安全（DB 层 `WHERE revoked_at IS NULL` + `requireAffected` 原子撤销兜底，Redis 仅快速校验被 DB 兜住），无需改造。
5. **`PopularArticles` 不限状态**（`model/stats.go`）：已修复——SQL 加 `WHERE status='published' AND (scheduled_at IS NULL OR scheduled_at <= NOW())`，与前台可见性语义一致。

---

*注：本次审计为只读分析 + 修复，所有结论经静态分析与实际编译/测试/构建/配置校验验证。前端 echarts 改写后图表视觉需在运行时人工确认（折线图 PV/UV 双线、tooltip、坐标轴格式）。*
