# Notes of Ashen 项目审计报告

**审计日期**：2026-06-26
**审计范围**：全项目（后端 Go + 前端 React + 部署配置 + 接口一致性）
**审计深度**：全面深度审计，按 P0-P3 优先级分级

---

## 总体评价

项目整体工程质量较高。后端安全防护扎实（SQL 全参数化、鉴权完整、密钥 fail-closed、IP 信任链严谨、Token 旋转撤销完备），业务逻辑事务使用合理，首注册 admin 有双重并发保险，文章版本管理在更新前快照。前端在 XSS 防护、Token 存储、路由守卫方面设计良好。

本次审计**未发现 P0 级问题**（无安全漏洞、数据破坏、权限绕过、生产无法启动）。发现 P1 级问题 4 项、P2 级 14 项、P3 级 9 项，集中在接口类型标签、部署摩擦、文档同步和前端类型枚举化四个方向。

---

## P1 — 核心流程不可用 / 部署安全风险

### ✅ P1-1 [接口] RegisterReq.EmailCode 缺少 optional 标签，首个管理员注册可能被阻断

- **问题文件**：
  - `internal/types/types.go:16` — `EmailCode string json:"emailCode"`（无 `optional`）
  - `api/notes-of-ashen.api:42` — `EmailCode string json:"emailCode,optional"`（有 `optional`）
  - `frontend/src/pages/Register.tsx:99` — `emailCode: requiresEmailCode ? emailCode.trim() : undefined`
- **问题描述**：`.api` 文件将 `EmailCode` 标注为 `optional`，但运行时类型源 `types.go` 没有 `optional` 标签。AGENTS.md 约定「当用户表为空且邮箱服务关闭时，首个管理员注册可跳过邮箱验证码」，logic 层通过 `RegistrationEmailCodeRequired(isFirstUser, emailEnabled)`（`logicutil/common.go:28`，返回 `!isFirstUser || emailEnabled`）正确实现了该逻辑。但前端在不需要验证码时发送 `emailCode: undefined`，`JSON.stringify` 会省略该 key，go-zero 对缺失的非 optional string 字段返回 `field "emailCode" is not set` 的 400 错误，请求根本到不了 logic 层。
- **潜在影响**：邮箱服务关闭场景下首个 admin 无法完成注册，系统无法初始化。
- **修复建议**：在 `types.go` 的 `RegisterReq.EmailCode` 标签中添加 `,optional`，与 `.api` 文件和 logic 行为对齐。Logic 层无需改动。

### ✅ P1-2 [部署] Meilisearch 容器健康检查使用了镜像中不存在的 wget

- **问题文件**：`docker-compose.yml:112-113`
- **问题描述**：健康检查命令 `wget -qO- http://127.0.0.1:7700/health | grep -q available` 依赖 `wget`，但 `getmeili/meilisearch:v1.13` 镜像基于 `debian:bookworm-slim`，仅预装 `ca-certificates curl libssl3 openssl`，不含 `wget`。命令返回 127（command not found），健康检查永远失败。
- **潜在影响**：Meilisearch 容器始终标记为 unhealthy，误导运维判断，1Panel 或外部编排器可能对 unhealthy 容器执行重启/告警。
- **修复建议**：将 `wget` 改为 `curl -sf http://127.0.0.1:7700/health | grep -q available`。

### ✅ P1-3 [部署] 两个 Dockerfile 均以 root 用户运行容器

- **问题文件**：`Dockerfile.api:18-32`、`Dockerfile.web:15-24`
- **问题描述**：`Dockerfile.api` 的 final stage（`alpine:3.22`）无 `USER` 指令，容器以 root 运行 Go 二进制；`Dockerfile.web` 的 `nginx:1.27-alpine` master 进程也以 root 运行。
- **潜在影响**：若存在 RCE 漏洞或 nginx 配置缺陷，攻击者直接获得容器内 root 权限，可横向访问 Docker 网络内的 Redis、Meilisearch。
- **修复建议**：API final stage 创建非 root 用户（`adduser -D -u 10001 appuser` + `USER appuser`）；Web 改用 `nginxinc/nginx-unprivileged` 镜像或绑定非特权端口后切换 `USER nginx`。

### ✅ P1-4 [接口] /healthz 文档描述与实现不一致

- **问题文件**：
  - `docs/API.md:101` — "返回 200 OK（纯文本 ok）"
  - `internal/handler/site/healthz.go:18-22` — 返回 `application/json`，状态码由 `HTTPStatus(report)` 决定（200 或 503）
  - `internal/logic/site/health.go:29-48` — 返回 `{ "status": "ok"|"degraded", "checks": { "db": {...}, "redis": {...} } }`
- **问题描述**：文档声称返回纯文本 `ok`，实际返回 JSON 健康报告，且依赖 down 时返回 503 而非 200。
- **潜在影响**：依赖文档编写的探针配置可能因 Content-Type 或状态码不匹配而误判健康状态。
- **修复建议**：更新 `docs/API.md` 的 `/healthz` 描述，说明返回 JSON 格式及 200/503 语义。

---

## P2 — 接口/类型/文档不一致 / 部署摩擦 / UX

### ✅ P2-1 [接口] UpdateSiteSettingsReq.HomeArticleLayout 三方 optional 不一致

- **问题文件**：`internal/types/types.go:71`（无 optional）vs `frontend/src/types/api.ts:62`（optional）vs `docs/API.md:573`（必填）
- **问题描述**：后端类型和文档标注必填，前端标注可选，但 logic（`site.go:48-51`）实现为空值回退当前值（可选语义）。
- **修复建议**：`types.go` 的 `HomeArticleLayout` 标签添加 `,optional`，同步更新 `docs/API.md`。

### ✅ P2-2 [接口] 前端 Update 类型使用 Partial<> 与后端全量字段不匹配

- **问题文件**：`frontend/src/types/api.ts:189,246,255`（`Partial<CreateXxxReq>`）vs `internal/types/types.go:201-216,315-319`（required 字段）
- **问题描述**：前端将更新接口定义为 `Partial<>` 使所有字段可选，但后端创建和更新共用同一类型，`title/slug/content/name` 等字段无 `optional`。`docs/API.md:387` 也声明「完整字段」。
- **修复建议**：前端更新类型移除 `Partial<>`，或后端为更新定义独立类型。

### ✅ P2-3 [接口] 错误码 50300（ServiceUnavailable）未文档化

- **问题文件**：`internal/errors/errors.go:43-45`（code 50300）、`internal/middleware/ratelimit.go:73,88`（使用）、`docs/API.md:53-61`（缺）
- **修复建议**：在 `docs/API.md` 错误码表添加 `| 50300 | 503 | 服务暂时不可用（限流依赖故障） |`。

### ✅ P2-4 [接口] ArticleLikeResp / SearchReindexResp 前端使用内联类型

- **问题文件**：`frontend/src/api/article.ts:29,72`（内联）vs `internal/types/types.go:298-301,310-313`（有定义）
- **修复建议**：在 `frontend/src/types/` 添加对应接口并引用。

### ✅ P2-5 [接口] CaptchaReq.purpose 后端 optional 但前端 required

- **问题文件**：`internal/types/types.go:33`（optional）vs `frontend/src/types/api.ts:38`（required）
- **修复建议**：前端改为 `purpose?: CaptchaPurpose`。

### ✅ P2-6 [部署] Meilisearch 容器始终启动，即使搜索功能关闭

- **问题文件**：`docker-compose.yml:101-127`、`:52`（`APP_SEARCH_ENABLED` 默认 false）、`:106`（`APP_MEILISEARCH_API_KEY` 用 `:?` 必填）
- **问题描述**：搜索默认关闭，但 meilisearch 容器无条件启动，且 production 模式强制要求 master key，用户必须为不用的服务生成密钥。
- **修复建议**：使用 Compose `profiles: ["search"]` 条件启动。

### ✅ P2-7 [部署] APP_RABBITMQ_URL 在 compose 中必填，即使 RabbitMQ 被禁用

- **问题文件**：`docker-compose.yml:57`（`:?`）、`:56`（`APP_RABBITMQ_ENABLED` 默认 true）
- **问题描述**：compose 层 `:?` 不感知 `ENABLED` 的值，禁用时仍需填假 URL。
- **修复建议**：将 `:?` 改为 `:-`，应用层 `ValidateConfig` 已正确处理。

### ✅ P2-8 [部署] APP_REDIS_ADDR 缺少 :? 必填标记

- **问题文件**：`docker-compose.yml:49`
- **问题描述**：其他关键依赖均用 `:?`，Redis 缺失，不 fail-fast。
- **修复建议**：统一使用 `:?`。

### ✅ P2-9 [部署] web 容器缺少健康检查

- **问题文件**：`docker-compose.yml:4-29`
- **修复建议**：添加 `healthcheck: test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:80/ || exit 1"]`（nginx:alpine 含 wget）。

### ✅ P2-10 [部署] .env.example 默认 APP_RABBITMQ_ENABLED=true，但 compose 无 RabbitMQ 服务

- **问题文件**：`.env.example:27`、`docker-compose.yml`（无 rabbitmq 服务）
- **问题描述**：默认开启但无容器，应用启动后 `mq.NewPublisher` 连接失败，无限重连刷错误日志。
- **修复建议**：默认改为 `false`，需要时显式开启。

### ✅ P2-11 [部署] compose 不包含 MySQL/Redis 服务，且未挂载数据库初始化脚本

- **问题文件**：`docker-compose.yml`、`deploy/mysql/schema.sql`
- **问题描述**：MySQL/Redis 需外部提供（1Panel 托管），`schema.sql` 需手动执行。
- **修复建议**：提供 `docker-compose.full.yml` override 或在 README 明确手动初始化步骤。

### ✅ P2-12 [前端] 状态/角色等字段使用 string 而非枚举

- **问题描述**：文章 status、用户 role、邮箱 purpose 等在前后端均以裸 string 传递，缺乏联合类型约束，易产生拼写漂移。
- **修复建议**：前端用字面量联合类型枚举化，后端可考虑定义具名常量。

### ✅ P2-13 [前端] 仍有硬编码文案未走 i18n

- **问题描述**：近期做了大量 i18n polish，但仍有零散硬编码中文/英文文案。
- **修复建议**：统一走 i18n key。

### ✅ P2-14 [前端] 部分交互元素点击区域不足 44×44px

- **问题描述**：近期 a11y polish 后仍有部分小图标按钮不满足 44×44px 触摸目标。
- **修复建议**：补齐点击区域。

---

## P3 — 性能 / 维护性 / 局部 polish

### ✅ P3-1 [接口] error.ts fieldNames 缺少 purpose 字段
- **文件**：`frontend/src/utils/error.ts:36-75`
- 后端返回 `"purpose is invalid"` 时翻译为半本地化 `"purpose不合法"`。建议添加 `purpose: localized('验证用途', 'Purpose')`。

### ✅ P3-2 [接口] ArticleVersionListResp.TagIDs 无 omitempty
- **文件**：`internal/types/types.go:396`
- 列表场景 nil 时输出 `"tagIds": null`，前端 `number[]` 不接受 null。建议添加 `,omitempty` 或 logic 返回空切片。

### ✅ P3-3 [部署] nginx 未启用 gzip 压缩
- **文件**：`deploy/nginx/default.conf`
- 建议 `gzip on; gzip_types text/css application/javascript application/json;`。

### ✅ P3-4 [部署] nginx 未隐藏版本号、未设置限流
- **文件**：`deploy/nginx/default.conf`
- 建议 `server_tokens off;` 和基础 `limit_req_zone`。

### ✅ P3-5 [部署] Docker 镜像使用 :latest tag
- **文件**：`docker-compose.yml:5,32`
- 无法回滚。建议用语义化版本或 git commit hash。

### ✅ P3-6 [部署] add_site_settings.sql 建表 VARCHAR(255) 与 schema.sql MEDIUMTEXT 不一致
- **文件**：`deploy/mysql/add_site_settings.sql:5` vs `deploy/mysql/schema.sql:22`
- 建议增量脚本直接用 `MEDIUMTEXT`。

### ✅ P3-7 [部署] Dockerfile.api 构建上下文包含前端源码
- **文件**：`Dockerfile.api:15`、`.dockerignore`
- 建议为 API 构建排除 `frontend/`。

### ✅ P3-8 [部署] APP_AUTH_COOKIE_SECURE 默认 true，本机 HTTP 开发时 Cookie 不保存
- **文件**：`.env.example:15`、`etc/notes-of-ashen.yaml:19`
- 生产默认安全，但本机 HTTP 开发不便。建议提供 override 模板预设 false。

### ✅ P3-9 [前端] 冗余/过时依赖
- **文件**：`frontend/package.json`
- 建议清理未使用依赖。

---

## 审计中确认的良好实践（无需修复）

| 维度 | 评价 |
|------|------|
| SQL 注入防护 | model 层全部参数化查询，动态拼接仅限白名单常量（`articleSelectFields`/`articleDisplayOrder`/`articleWhere` clauses），LIKE 通配符经 `escapeLike` 转义 |
| 认证授权 | 路由分组清晰，所有 admin 接口过 `authRequired`，角色鉴权在 logic 层用 `RequireAdmin`/`RequireContentManager` 统一完成 |
| 密钥安全 | `ValidateConfig` fail-closed 拦截占位符（`change-me`/`replace-with`/`<replace`），强制 `AccessSecret` ≥16 字符，AI Key 加密密钥校验 |
| IP 信任链 | `httphelper` 先验证 `RemoteAddr` 命中可信 CIDR 才采用 `X-Forwarded-*`，XFF 从右向左取首个不可信 IP，有完整测试 |
| Token 安全 | refresh token 旋转（用后撤销），DB 为准 + Redis 缓存降级，禁用用户即时撤销，登出归属不一致时记录安全事件 |
| 防枚举 | 登录用户不存在/密码错误返回相同错误信息 |
| 限流 | 登录/注册/验证码/密码重置/改密均覆盖 fail-closed 限流，captcha 30/min，AI 20/min |
| 首注册 admin 并发 | `WithUserRegistrationLock`（GET_LOCK）+ `AdminExists` DB 双保险，避免并发多 admin |
| 文章版本管理 | `UpdateArticle` 在事务内先 `createArticleVersion` 快照再更新，`RestoreArticleVersion` 在事务内恢复 |
| 事务一致性 | 文章/分类/标签/站点设置/简历项目的增删改均包在 `WithTx` 内，标签关联替换与主表同事务 |
| 缓存降级 | auth 用户缓存 Redis 故障 fail-open 直查 DB，refresh token 缓存缺失以 DB 为准 |
| 降级路径 | RabbitMQ 禁用/失败回退同步落库；Search 禁用全 no-op；Email 禁用返回 Forbidden；AI 禁用跳过校验 |
| 统一响应 | 所有 JSON handler 统一用 `response.Ok`/`NoData`/`ErrorCtx` |
| 配置一致性 | `.env.example`、`etc/notes-of-ashen.yaml`、`internal/config` 三方字段名/默认值/语义高度一致 |
| .gitignore / .dockerignore | 正确忽略 `.env`/`data/`/二进制/`node_modules`，保留 `.env.example` |
| schema.sql 幂等 | `IF NOT EXISTS` + `ON DUPLICATE KEY UPDATE`，字符集统一 `utf8mb4_0900_ai_ci` |
| nginx 安全头 | 设置了 `X-Content-Type-Options`/`X-Frame-Options`/`Referrer-Policy` |
| 端口暴露 | web 仅绑 `127.0.0.1:1270`，api/meilisearch 仅 `expose`，无多余端口 |

---

## 修复优先级建议

按影响和实施成本排序，建议按以下顺序处理：

1. **P1-1** EmailCode optional 标签（一行改动，解除首注册阻断）
2. **P1-2** Meilisearch healthcheck wget→curl（一行改动）
3. **P1-3** Dockerfile 非 root USER（安全加固）
4. **P1-4** /healthz 文档更新（文档同步）
5. P2-7/8/10 部署必填项与默认值修正（降低首次部署摩擦）
6. P2-1/2/5 接口类型标签对齐（防止类型漂移触发 400）
7. 其余 P2/P3 按需处理

---

## 本轮修复记录（2026-06-26）

本轮对全部 27 项审计发现完成闭环：历史提交已修复 7 项（P1-1、P1-2、P1-4、P2-1、P2-3、P2-4、P2-5），本轮修复 17 项，部分修复项补齐残留 3 项（P2-11 视 README 已有说明为完成、P2-12 补齐 ArticleStatus/UserRole/UserStatus 枚举化、P2-13 补齐残留 aria-label 与标题 i18n）。所有项标题已标注 ✅。

### 本轮实际改动

**后端**
- `internal/types/types.go`：`ArticleVersionResp.TagIDs` 加 `,omitempty`（P3-2）

**部署 / Docker**
- `Dockerfile.api`：final stage 新增非 root 用户 `appuser`（uid 10001）+ `USER appuser`（P1-3）
- `Dockerfile.web`：改用 `nginxinc/nginx-unprivileged:1.27-alpine`，`EXPOSE 8080`，COPY `10-limits.conf` 到 conf.d（P1-3）
- `deploy/nginx/default.conf`：`listen 8080`、`server_tokens off`、`gzip` 压缩、`/api/` 加 `limit_req`（P1-3 联动 + P3-3 + P3-4）
- `deploy/nginx/10-limits.conf`（新建）：`limit_req_zone` 定义（P3-4）
- `docker-compose.yml`：镜像 tag 改 `${IMAGE_TAG:-latest}`、meilisearch 加 `profiles:["search"]`、`APP_REDIS_ADDR` 加 `:?`、web 加 healthcheck、web 端口改 8080（P3-5 + P2-6 + P2-8 + P2-9 + P1-3 联动）
- `.env.example`：`APP_RABBITMQ_ENABLED` 默认 false、新增 `IMAGE_TAG`、新增 COOKIE_SECURE dev override 注释行（P2-10 + P3-5 + P3-8）
- `deploy/mysql/add_site_settings.sql`：`setting_value` 改 `MEDIUMTEXT`、字符集对齐 `utf8mb4_0900_ai_ci`（P3-6）
- `Dockerfile.api.dockerignore`（新建）：API 构建排除 `frontend/`（P3-7）

**前端**
- `frontend/src/types/api.ts`：3 个 Update 类型移除 `Partial<>` 展开为显式可选字段；`UpdateUserStatusReq/UpdateUserRoleReq/UpdateArticleStatusReq/ArticleListParams/CreateArticleReq.status` 改用联合类型（P2-2 + P2-12）
- `frontend/src/types/index.ts`：新增 `UserRole`/`UserStatus`/`ArticleStatus` 联合类型，替换 `User.role/status`、`Article.status`、`ArticleVersion.status`（P2-12）
- `frontend/src/api/article.ts`、`api/user.ts`：对应函数签名改用联合类型（P2-12）
- `frontend/src/pages/admin/Articles.tsx`、`ArticleEditor.tsx`、`Users.tsx`：state 与 handler 类型对齐联合类型（P2-12）
- `frontend/src/utils/error.ts`：`fieldNames` 加 `purpose`（P3-1）
- `frontend/src/components/ui/Tag.tsx`、`InlineNotice.tsx`：关闭按钮补 `min-w/min-h-[44px]`、aria-label 走 i18n（P2-13 + P2-14）
- `frontend/src/pages/admin/AdminLayout.tsx`、`components/ProjectPreviewModal.tsx`：硬编码 aria-label/标题走 i18n（P2-13）
- `frontend/src/i18n.ts`：新增 `common.remove`/`common.dismiss`/`common.breadcrumb` key
- `frontend/package.json`：移除冗余 `@typescript-eslint/eslint-plugin` + `@typescript-eslint/parser`（v6），统一 `typescript-eslint` v7（P3-9）

### 验证

- `go build ./...` ✅
- `go test ./internal/logic/article/... ./internal/logic/auth/...` ✅
- `pnpm install`（lockfile 已移除 v6 包）→ `pnpm lint` ✅ → `npx tsc --noEmit` ✅ → `pnpm build` ✅
- `docker compose config` ✅（web 端口 8080、healthcheck、profile 隔离均生效）
- `docker compose --profile search config --services` 确认 meilisearch 仅在 search profile 下启动 ✅
- `nginxinc/nginx-unprivileged` 容器内 `nginx -t` ✅（gzip/server_tokens/limit_req 语法正确）

---

*注：本次为只读审计，未运行 `go test`/`pnpm build`/`docker compose config`。后端安全与业务逻辑部分由手动检查关键文件完成（agent 超时），前端与部署部分由子代理完成。所有结论基于静态分析。*
